#include "server.h"

// cJSON *mx_search_user_in_db(sqlite3 *db, gchar *str_search) {
    // sqlite3_str *sql_str = sqlite3_str_new(db);
    // gint32 rv =SQLITE_OK;
    // gchar *request = NULL;
    // sqlite3_stmt *stmt;

    // sqlite3_str_appendf(sql_str, "select * from users where name like '%s%'", 
    //                     str_search);
    // request = sqlite3_str_finish(sqlite_str);
    
    // sqlite3_free(request);
    
// }

void mx_search_room_in_db(gchar *search) {
    // db++;
    search++;
}

// cJSON *mx_search_user_in_db(sqlite3 *db, gchar *search) {
// }


